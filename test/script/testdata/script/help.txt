# root help stays stable and readable in a real process
exec $MUSHER_BIN --no-color --no-input --help
stdout 'Discover, load, publish, and manage agent bundles'
stdout 'Authoring:'
stdout 'bundle      Create, manage, and publish bundles'
