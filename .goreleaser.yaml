# GoReleaser configuration for Musher CLI
# Documentation: https://goreleaser.com

version: 2

project_name: musher

before:
  hooks:
    - go mod tidy

builds:
  - id: musher
    main: ./cmd/musher
    binary: musher
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}

archives:
  - id: default
    formats: [ 'tar.gz' ]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [ 'zip' ]
    files:
      - README.md
      - LICENSE*

checksum:
  name_template: "checksums.txt"

changelog:
  disable: true

release:
  github:
    owner: musher-dev
    name: musher-cli
  draft: false
  mode: keep-existing
  prerelease: auto
  name_template: "{{.Version}}"
  extra_files:
    - glob: install.sh
